services:
  - type: web
    name: kaijiang-analysis
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        sync: false  # Render 会自动注入 PORT
